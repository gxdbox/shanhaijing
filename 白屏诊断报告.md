# shanhaijing 白屏问题诊断报告

日期：2026-09-01
任务：复现「本地启动后什么都没有」并收集启动日志

---

## 一、环境信息

| 项 | 值 |
|---|---|
| 操作系统 | macOS 26.5.1 (Mac mini) |
| Cocos Creator | 3.8.8（/Applications/Cocos/Creator/3.8.8/CocosCreator.app） |
| Cocos Dashboard | 2.2.1（运行中，PID 50367） |
| 项目路径 | /Users/pony/Documents/game/shanhaijing |
| 项目版本 | package.json: creator 3.8.8 |
| 内存 | 16 GB |
| 磁盘 | 108 GB 可用（充足） |

## 二、复现方式与结果

1. 打开 Cocos Dashboard → 项目 → 启动 shanhaijing（编辑器会加载项目并自动导入资源）。
2. 编辑器加载完成后进行构建（web-mobile）→ 产物在 build/web-mobile。
3. 用 `python3 -m http.server 8099` 静态托管 build/web-mobile，Chrome 打开 http://127.0.0.1:8099/index.html。

**结果：白屏。** 页面只剩一个 canvas，无任何游戏画面。

---

## 三、浏览器实测日志（Console / Network / Sources）

### Console（白屏时浏览器控制台）
```
[bullet]:bullet wasm lib loaded.
[PHYSICS2D]: register box2d.
[PHYSICS]: register bullet.
[PHYSICS]: using bullet.
Init SubSystem: 97.177001953125 ms
Cocos Creator v3.8.8
ERROR: TypeError: Cannot read properties of undefined (reading 'getBinding')
        at .../cocos-js/_virtual_cc-0OximA6Z.js:50192:20
```

- 引擎初始化完成，打出 "Cocos Creator v3.8.8"。
- 随后在 **Init Project 阶段** 抛 `TypeError: Cannot read properties of undefined (reading 'getBinding')`，启动流程中断，游戏永不渲染。
- 该错误位于引擎内部 `Game.init()` 的项目初始化 Promise 链中：加载项目插件 jsList / 场景资源时，因 **main bundle 中没有任何场景/脚本资源**，导致内部 `getBinding` 拿到 undefined。

### Network（抓取到的全部加载请求）
```
src/polyfills.bundle.js
src/system.bundle.js
src/import-map.json
cocos-js/cc.js
cocos-js/_virtual_cc-0OximA6Z.js
src/settings.json
cocos-js/bullet.release.wasm-*.js / *.wasm / spine.* / effect.bin
assets/internal/config.json      ← 只加载了 internal bundle
assets/internal/index.js
```
**关键：没有任何 `assets/main/*`、`assets/resources/*` 的加载请求。**
启动后只加载了 internal（引擎内置资源），main/resources 两个项目 bundle 完全没有被请求，自然是空白。

### DOM 状态
- #GameDiv / #GameCanvas 存在，canvas 尺寸 1280x577。
- canvas 内容为空（引擎未开始渲染）。

---

## 四、构建产物证据（为什么 bundle 是空的）

### 1. main / resources bundle 的 config.json 全部为空
`build/web-mobile/assets/main/config.json`：
```json
{
  "name": "main",
  "deps": [],
  "uuids": [],
  "paths": {},
  "scenes": {},
  "packs": {},
  "versions": { "import": [], "native": [] },
  "redirect": []
}
```
resources/config.json 同样为空；`assets/main/import` 目录不存在（正常非空项目应有一堆 uuid 命名的导入资源文件）。构建产物壳子齐全，但内容为 0。

### 2. 构建日志明示「0 场景、0 脚本」
`temp/builder/log/web-mobile9-1-2026 09-09.log`（09:09 构建）：
```
Start build task, options: { ... "startScene":"8cb9d144-29c2-6bc8-b729-d851480148ab",
  "scenes":[{"url":"db://assets/scenes/main.scene","uuid":"8cb9d144-..."}] ... }
  Number of all scenes: 0
  Number of all scripts: 0
  Number of other assets: 1
Error: Exit process with code:null, signal:SIGTERM in task build-script
```
- 构建参数里明明带了 startScene 和 scenes。
- 但 asset-db 查出来 **scenes: 0、scripts: 0**——即资源数据库里根本不认识这些资源。
- 脚本打包子进程被 **SIGTERM** 终止，build-script 任务失败/空跑。

### 3. 构建过程极快（1~2 秒）——符合「空构建」特征
`temp/builder/log/web-mobile9-1-2026 09-08.log`：整个 build 只有约 2 秒，没有任何真实资源处理（image compress task 0、json group 全部空）。正常带场景+脚本的项目构建不会这么快。

### 4. project.log 的直面报错
`temp/logs/project.log`（09:09 启动）：
```
error: The selected scene does not exist or is in a Bundle and cannot be set as the Start Scene.
error: Check build parameter startScene (undefined) failed, The selected scene does not exist or is in a Bundle and cannot be set as the Start Scene.
warn: Check build parameter mainBundleCompressionType (merge_dep) failed, compression type(undefined) is invalid...
```
（09:01 启动时同样出现，09:09 重复出现）

---

## 五、根因（最可疑，证据充分）

### 所有 .meta 文件的 importer 被批量破坏
`git diff` 显示项目里 **29 个 .meta 文件全部被改动**：

| 文件 | git 里（正常） | 工作区（当前） |
|---|---|---|
| assets/scenes/main.scene.meta | `"importer": "scene", "ver": "1.1.50"` | `"importer": "*", "ver": "0.0.1"` |
| assets/scripts/**/*.ts.meta | `"importer": "typescript", "ver": "4.0.24"` | `"importer": "*", "ver": "0.0.1"` |
| assets/resources.meta | `"importer": "directory", "isBundle": true, "bundleName": "resources"` | `"importer": "*"`（userData 丢失，bundle 声明没了） |
| 所有目录 .meta | `"importer": "directory"` | `"importer": "*"` |

- `.meta` 是 Cocos 的资源类型声明文件，`importer` 字段决定资源怎么被导入、识别、编译。
- 所有 importer 被改成 `*`（未知/兜底类型）后，asset-db 无法把 main.scene 识别为「场景」、无法把 .ts 识别为「脚本」、无法把 resources 识别为「Bundle」。
- 后果链：资源数据库里 **场景数=0、脚本数=0 → 构建产物 bundle 全空 → 浏览器启动时 main bundle 无场景可加载 → 引擎 getBinding undefined → 白屏**。

### 触发时间点
- 工作区 .meta 修改时间：**Sep 1 09:09:25**
- 09:01、09:09 的构建都失败（前后一致）。
- 即最近一次编辑（09:09 前后）把 meta 写坏了。资源内容本身（main.scene / GameRoot.ts / MapView.ts）git diff 显示只是正常小改动（相机 z 轴），不是根源。

### 次要观察（非根因，可后续留意）
1. build-script 子进程 SIGTERM——可能由 meta 破坏导致脚本识别失败后的连锁；也可能是编辑器资源池异常的表现。
2. 资源库 library/.assets-*.json 里所有资源的 value 都是 `{}`（无类型元数据），与 importer=* 一致。
3. resources 目录下 textures/beasts 为空目录（本来就没有贴图文件，不影响白屏主因）。

---

## 六、结论与建议（验证步骤）

**根因：项目 assets 下所有 .meta 文件的 `importer` 字段被批量误改为 `*`（ver 也被重置为 0.0.1），导致 Cocos asset-db 无法识别场景/脚本/资源目录，构建出空 bundle，游戏启动后白屏报 getBinding 错误。**

建议的下一步（定位/修复）：
1. **恢复 .meta 文件**：用 git 恢复（`git checkout -- assets/` 保留源码改动，或逐个从 git HEAD 恢复 meta），关键恢复项：
   - main.scene.meta → importer=scene, ver=1.1.50
   - 各 .ts.meta → importer=typescript, ver=4.0.24
   - resources.meta → importer=directory + userData.isBundle=true/bundleName=resources
   - 目录 .meta → importer=directory
2. 删除 library/ 与 temp/ 缓存，重新打开项目让 asset-db 重新导入。
3. 重新构建 web-mobile，确认 config.json 有 scenes/uuids、main/import 目录生成。
4. 重开浏览器验证画面正常。
5. 排查「为什么会批量改 meta」：大概率是某次编辑器异常/外部脚本批量处理导致，后续注意不要用脚本覆盖 .meta。

（如需，我可以直接执行第 1~3 步修复。）